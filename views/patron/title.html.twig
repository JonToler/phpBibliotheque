<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <link rel="stylesheet" href="/css/bootstrap.min.css" type="text/css">
        <link rel="stylesheet" href="/css/styles.css" type="text/css">
        <script src="/js/jquery-3.1.1.min.js" type="text/javascript"></script>
        <script src="/js/bootstrap.min.js" type="text/javascript"></script>
        <script src="/js/scripts.js" type="text/javascript"></script>
        <title>It's NOT pronounced "Lie-Berry"!</title>
    </head>
    <body>
        <div class="container">
            <div class="jumbotron">
                <h1><a href="/">Library</a></h1>
            </div>
            <h3>{{ title.getName }}</h3>
            <hr>
            {% for author in authors %}
                <h4>{{ author.getName }}</h4>
            {% endfor %}
            <hr>
            <h4>{{ title.getCopies|length }} Copies in Library</h4>
            <h4>{{ title.onLoanList[1]|length }} Copies Available</h4>
            <hr>
            <h3>Check Out</h3>
            <form action="/patron/titles/{{ title.getId }}/checkout" method="post">
                <div class="form-group">
                    <input id="title_id" name="title_id" type="hidden" value="{{ title.getId }}">
                    <select id="patron_id" name="patron_id" class="form-control">
                        {% if patrons is not empty %}
                            {% for patron in patrons %}
                            <option value="{{ patron.getId }}">{{ patron.getName }}</option>
                            {% endfor %}
                        {% endif %}
                    </select>
                </div>
                <button type="submit" class="btn btn-default">Check Out a Copy to Patron</button>
            </form>
            <p><a href="/patron">Return to the Patron Main Menu</a></p>
        </div>
    </body>
</html>
