<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <link rel="stylesheet" href="/css/bootstrap.min.css" type="text/css">
        <link rel="stylesheet" href="/css/styles.css" type="text/css">
        <script src="/js/jquery-3.1.1.min.js" type="text/javascript"></script>
        <script src="/js/bootstrap.min.js" type="text/javascript"></script>
        <script src="/js/scripts.js" type="text/javascript"></script>
        <title>It's NOT pronounced "Lie-Berry"!</title>
    </head>
    <body>
        <div class="container">
            <div class="jumbotron">
                <h1><a href="/">Library</a></h1>
            </div>
            <h3>Loan History</h3>
            <ul class="list-group">
                <li class="list-group-item active"><h4>Titles</h4></li>
                {% if titles is not empty %}
                    {% for title in titles %}
                    <li class="list-group-item"><a href="/patron/titles/{{ title.getId }}">{{ title.getName }} |                        {% if title.getAuthor is not empty %}
                            {% for author in title.getAuthor %}
                                {{ author.getName }} |
                            {% endfor %}
                        {% endif %} ({{ title.getCopies|length }} Copies in Library, {{ title.onLoanList[1]|length }} Copies Available)</a></li>
                    {% endfor %}
                {% endif %}
            </ul>
            <p><a href="/patron">Return to the Patron Main Menu</a></p>
        </div>
    </body>
</html>
